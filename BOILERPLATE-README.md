# BOILERPLATE-README.md - 이 프로젝트가 여러분 대신 정해둔 것들

이 문서는 **수업 시간에는 설명하지 않는다.** 오늘 실습에 필요한 만큼만 머리에 담게 하려고, 이미 정해 둔 것들은 설명을 생략했다 - 그 대신 궁금해질 때 혼자 찾아 읽을 수 있게 여기에 적어 둔다.

**쓰는 법**: Ctrl+F(맥은 Cmd+F)로 아래 코드(B-01 같은)나 궁금한 단어로 바로 찾는다. 위에서부터 순서대로 읽을 필요는 없다.

## 색인

- [B-01] 폴더가 3개 파일로 고정돼 있다
- [B-02] 모델이 haiku로 고정돼 있다
- [B-03] 비밀 값(키)은 코드 밖에 있다
- [B-04] RLS가 켜져 있다 - 넣기만 허용
- [B-05] .gitignore 목록이 미리 채워져 있다
- [B-06] 단축 명령 4개가 대신해 주는 일
- [B-07] 시작 환경 통일(/start)이 확인하는 것
- [B-08] summary "(요약 대기)" fallback이 예정돼 있다

---

## [B-01] 폴더가 3개 파일로 고정돼 있다

**무엇이 정해져 있나** `index.html`(신청 폼 화면) · `api/submit.js`(저장 서버 코드) · `setup.sql`(표 설계도). 이 세 파일 밖으로 구조가 넓어지지 않는다 - 새 폴더, 새 프레임워크, 컴포넌트 분리 같은 것이 없다.

**왜 그렇게 했나** 화구가 3개인 주방에서 요리를 먼저 익히는 것과, 화구를 10개로 늘리는 것은 다른 일이다. 오늘 배울 것은 "폼-서버-표가 어떻게 맞물리는가"이지 "프로젝트를 어떻게 조직하는가"가 아니다. 화구 개수(파일 구조)를 고정해 둬야 요리(핵심 개념)에 집중할 수 있다.

**내가 바꾸고 싶어지면** 나중에 파일이 여러 개로 늘어나는 실제 프로젝트를 만드는 것은 자연스러운 일이다 - 이 3파일 구조를 "항상 이래야 한다"는 규칙으로 오해하지 않는다. 지금은 3개로 충분한 것을 배우는 단계일 뿐이다.

## [B-02] 모델이 haiku로 고정돼 있다

**무엇이 정해져 있나** 이 폴더에서 Claude Code(`claude`)를 켜면 항상 haiku 모델로 시작한다. `.claude/settings.json` 안의 `{"model": "haiku"}` 한 줄 때문이다.

**왜 그렇게 했나** 오늘 시키는 일("질문 하나 추가해줘" 같은)은 작고 명확하다. 작은 배달 하나 시키는 데 대형 트럭을 부를 필요는 없다 - 빠르고 저렴한 모델로 충분하고, 오히려 더 빠르게 끝난다.

**내가 바꾸고 싶어지면** `.claude/settings.json`을 지우거나 값을 바꾸면 다른 모델로도 켤 수 있다 - 다만 오늘 실습 범위에서는 그럴 이유가 없다. 나중에 더 복잡한 작업(예: 4주차 AI 기능 개발)을 할 때 모델 선택은 그때 다시 판단하면 된다.

## [B-03] 비밀 값(키)은 코드 밖에 있다

**무엇이 정해져 있나** `SUPABASE_URL`과 `SUPABASE_ANON_KEY`는 `index.html`이나 `api/submit.js` 안에 직접 적혀 있지 않다. 로컬에서는 `.env` 파일에, 배포된 곳에서는 Vercel의 Environment Variables 화면에만 있다.

**왜 그렇게 했나** 열쇠를 현관문에 붙여 두면 누구나 문을 열 수 있다. 코드는 여러 사람이 보기도 하고 어딘가에 올라가기도 한다 - 그 안에 열쇠(키)가 그대로 있으면, 코드를 본 모두가 내 Supabase 안으로 들어올 수 있게 된다.

**내가 바꾸고 싶어지면** 절대 코드 안에 키를 직접 써넣지 않는다. 확인차 잠깐 넣어 보고 싶어져도 그 상태로 커밋하거나 배포하지 않는다 - 실수로 넣었다면 바로 지우고, 그 키는 발급처(Supabase Settings > API)에서 재발급(회전)한다.

## [B-04] RLS가 켜져 있다 - 넣기만 허용

**무엇이 정해져 있나** `setup.sql`이 `applications` 테이블에 RLS(Row Level Security)를 켜고, "익명 키로는 insert(넣기)만 허용" 정책을 넣어 뒀다. 이 폼으로 데이터를 넣을 수는 있어도, 이 앱이 쓰는 공개 키로 표를 통째로 읽거나 지울 수는 없다.

**왜 그렇게 했나** 우체통은 누구나 편지를 넣을 수 있지만, 그 안의 편지를 꺼내 읽는 것은 열쇠를 가진 사람(Supabase 대시보드에 로그인한 나)만 할 수 있어야 한다. RLS가 없으면, 이 폼의 공개 주소를 아는 누구나 다른 신청자의 이름과 연락처까지 전부 읽어갈 수 있게 된다.

**내가 바꾸고 싶어지면** 절대 스스로 끄지 않는다. "에러가 안 풀려서 RLS를 그냥 꺼버리고 싶다"는 순간이 오면, 그건 대부분 RLS 문제가 아니라 다른 원인(setup.sql 미실행, 정책 문구 오타 등)이다. CLAUDE.md 규칙에 따라 Claude Code는 끄기 전에 먼저 멈추고 확인을 받게 돼 있다.

## [B-05] .gitignore 목록이 미리 채워져 있다

**무엇이 정해져 있나** `.env` · `.env.local` · `node_modules/` · `.vercel` 네 항목이 이미 `.gitignore`에 들어 있다. git이 이 폴더와 파일들을 아예 추적하지 않게 막아 둔 것이다.

**왜 그렇게 했나** `.env`에는 키가 들어 있고, `node_modules/`는 용량만 큰 설치물이고, `.vercel`은 배포 도구가 만드는 로컬 캐시다 - 셋 다 "내가 쓴 코드"가 아니라 "내 컴퓨터에만 있어도 되는 것"이라 커밋할 이유가 없다.

**내가 바꾸고 싶어지면** 이 네 항목은 지우지 않는다. 새로운 비밀 파일(예: 다른 이름의 env 파일)을 만들면, 그 이름도 여기에 추가해야 한다. `/deploy`가 매번 노출 검사를 다시 하지만, `.gitignore` 자체가 첫 번째 방어선이다.

## [B-06] 단축 명령 4개가 대신해 주는 일

**무엇이 정해져 있나** `/start`(시작 환경 확인) · `/check`(종단 검증 순서 안내) · `/deploy`(비밀값 검사 + 커밋 + 배포) · `/fix`(에러 진단)가 `CLAUDE.md`에 정의돼 있다. 이 네 마디가 각각 여러 단계를 순서대로 대신 실행한다.

**왜 그렇게 했나** "먼저 이것 확인하고, 그다음 이거 하고, 마지막에 이거"를 매번 손으로 순서대로 치는 것은 실수가 나기 쉽고 지친다. 자주 반복하는 순서는 이름 하나로 묶어 두면 순서를 안 잊는다.

**내가 바꾸고 싶어지면** 이 네 명령이 정확히 어떤 단계를 실행하는지는 `CLAUDE.md` 원문에 전부 적혀 있다 - 궁금하면 그 파일을 열어 직접 읽으면 된다. 손으로 하나씩 쳐도 결과는 같다(순서를 안 잊어야 하고, 더 오래 걸릴 뿐).

## [B-07] 시작 환경 통일(/start)이 확인하는 것

**무엇이 정해져 있나** `/start`는 node·npm 버전, 지금 폴더가 맞는 폴더인지, `npm i` 결과, `.env.example` 존재 여부를 순서대로 확인하고, 마지막에 `[환경OK]` 영수증 한 줄을 출력한다. 하나라도 실패하면 `[환경막힘]` 줄을 대신 출력하고 멈춘다.

**왜 그렇게 했나** 여러 명이 각자 다른 컴퓨터로 시작하면, 누구는 node가 아예 없고 누구는 엉뚱한 폴더에서 명령을 치고 있다 - 이 차이를 강사가 화면 하나하나 눈으로 찾아내는 대신, "영수증 한 줄이 떴다"는 사실 하나로 "이 사람은 준비됐다"를 확인한다.

**내가 바꾸고 싶어지면** 영수증 대신 `[환경막힘]`이 뜨면, 그 줄에 적힌 항목과 원인을 그대로 채팅에 붙여넣는다. 스스로 원인을 추측해 다른 파일을 고치려 하지 않는다 - 원인 항목이 이미 그 줄에 적혀 있다.

## [B-08] summary "(요약 대기)" fallback이 예정돼 있다

**무엇이 정해져 있나** 지금 이 프로젝트 코드에는 이 문구가 없다. 다음 주(4주차, Haiku를 내 웹에 직접 붙이는 주)부터, AI 요약 호출이 실패하거나 늦어지면 화면이 빈 칸으로 남는 대신 "(요약 대기)"라는 문구가 뜨도록 미리 정해 두었다.

**왜 그렇게 했나** 음식점에서 주문한 메뉴가 늦어질 때 "곧 나옵니다"라는 안내가 있는 것과, 화면이 그냥 멈춘 것처럼 보이는 것은 완전히 다른 경험이다. AI 호출은 가끔 느리거나 실패할 수 있다(네트워크, 사용량 한도 등) - 그럴 때도 화면이 고장난 것처럼 보이지 않도록, "지금은 없지만 곧 채워질 수 있다"를 정직하게 보여주는 것이 이 문구의 역할이다.

**내가 바꾸고 싶어지면** 지금 당장 손댈 코드가 없다 - 4주차 자료에서 이 부분을 자세히 다시 설명한다. 지금은 "이런 표시가 나중에 나온다" 정도만 알아 두면 충분하다.

---

이 문서는 수업 진도와 무관하다 - 궁금할 때만 읽으면 된다.

## B-09. 왜 "하면 안 되는 것"을 두 번이나 점검하나요?

사고는 대부분 "몰라서"가 아니라 "알지만 그 순간 잊어서" 납니다. 그래서 사람이 기억하는 대신 /start(작업 시작)와 /deploy(배포 직전) 두 시점에 기계가 같은 목록을 다시 봅니다 - 시작할 때 깨끗했어도 작업 중에 비밀값이 코드에 들어갔을 수 있어서, 나가기 직전 한 번 더 보는 것입니다. 목록이 "해야 할 일"이 아니라 "하면 안 되는 일"인 이유도 같습니다 - 해야 할 일은 안 하면 티가 나지만, 하면 안 되는 일은 저지른 순간 티가 안 나고 나중에 사고로 나타납니다.

## B-10. ignore 파일이 두 개인 이유 (.gitignore / .vercelignore)

같은 "빼기 목록"이지만 빼는 곳이 다릅니다. `.gitignore`는 **기록(커밋)에서** 뺄 것 - 비밀값(.env)과 남의 컴퓨터에서 다시 만들어지는 것(node_modules). `.vercelignore`는 **배포(공개 서버)에서** 뺄 것 - 손님이 볼 필요 없는 문서(*.md)와 설정 폴더까지 뺍니다. 그리고 표(Supabase)에도 같은 원칙이 있습니다 - 화면으로 나갈 데이터만 넣고, 비밀값·개인 연락처 원문처럼 나가면 안 되는 것은 애초에 넣지 않습니다.

## B-11. /기록은 왜 "사실만" 적나요?

기록에는 두 종류가 있습니다 - 무슨 일이 있었나(사실)와 그래서 나는 무엇을 느꼈나(회고). 에이전트는 앞의 것을 잘하고, 뒤의 것은 여러분만 할 수 있습니다. 그래서 /기록은 오늘 실제로 바뀐 파일·해결된 문제·확인된 결과만 적고, 회고 네 줄은 빈 칸으로 남깁니다. 이 기록이 GitHub에 쌓이면 그대로 포트폴리오의 재료가 됩니다 - "이 사람이 무엇을 어떻게 만들었는지"를 사실로 보여주는 문서는 어떤 자기소개보다 힘이 셉니다. 그리고 기록마다 그날 만난 용어를 풀어 두는 이유는, 예비 PM에게 용어는 외우는 것이 아니라 자기 프로젝트의 장면과 함께 남을 때 진짜가 되기 때문입니다.

