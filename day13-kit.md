# Day13 실습 키트 - 복사해서 쓰는 문장 모음
<!-- spec_ref: 주차나선 SOT §9 D13 + 한얼 260804 확정(오전=상세 라우트, 오후=규칙판, day13-starter 독립실행) -->

오늘 수업에서 입력할 문장들입니다. 슬라이드와 번호가 같습니다. 그대로 복사해서 쓰시면 됩니다.

---

## 0. 아침 확인 (09:00)

1. 내 배포 주소의 `list.html`을 엽니다 - 판매 목록이 보이는지 확인합니다
2. `index.html`에서 주문 1건을 등록합니다 - 구매자 이름 칸에 `아침확인`
3. `list.html`을 새로고침해 방금 주문이 맨 위에 오는지 봅니다
4. #실습공유에 "어제 것 열림" 한 줄을 올립니다

**막히면 - 번호로 보고해 주세요**

| 번호 | 증상 | 복구 |
|---|---|---|
| 1 | 목록이 텅 빔 | 읽기 정책 또는 표가 없는 것 - day13-starter의 setup.sql을 SQL Editor에서 Run 한 번 |
| 2 | "환경변수가 설정되지 않았습니다" | Vercel Settings > Environment Variables 입력 후 재배포 |
| 3 | 주소가 아예 404 | `npx vercel --prod` 재배포 후 주소 재공유 |
| 4 | 어제 폴더 상태가 엉킴 | day13-starter로 갈아타기 (README-DAY13.md 갈래 2 - 5분) |

---

## 1. 오전 - 상세 페이지 지시 (Claude Code에 입력)

```text
판매 상세 페이지를 만들어줘.
1) detail.html을 새로 만들어서 주소가 detail.html?id=3 형식이면 그 판매 1건의 상세(구매자, 상품, 수량, 단가, 총액, 구매 시각)를 보여주게 해줘. api/list.js의 단건 조회(?id=)를 쓰면 돼.
2) list.html의 각 행에 그 상세 페이지로 가는 링크를 달아줘.
3) 상세 페이지에는 목록으로 돌아가는 링크도 넣어줘.
4) 끝나면 예시 링크 주소 하나를 알려줘
```

확인: 목록에서 한 건을 클릭 -> 주소창에 `?id=번호`가 남는 상세 페이지가 열립니다.

---

## 2. 오전 - 세 동작 체험 (결과를 그대로 적어 둡니다)

| 동작 | 결과 |
|---|---|
| 상세 페이지에서 새로고침 (Cmd/Ctrl+R) | |
| 상세 주소를 복사해 새 탭에 붙여넣기 | |
| 상세에서 뒤로가기 | |

세 동작이 전부 자연스럽게 되는 이유는 하나입니다 - **주소가 "어느 판매를 보는지"를 담고 있어서**입니다.

---

## 3. 오후 첫 순서 - 커밋 기록 보기 (Claude Code에 입력)

```text
지금까지의 커밋 기록을 보여줘
```

배포할 때마다 커밋이 자동으로 쌓여 왔습니다 - 각 줄이 "그때로 돌아갈 수 있는 지점"입니다. 이어서 GitHub 계정이 있는 분/없는 분을 확인합니다(있으면 마지막에 GitHub 올리기까지, 없으면 로컬 기록까지 - 기능 차이 없음).

---

## 3-1. 오후 - 규칙 카드 (하나를 골라 자기 말로 만듭니다)

아래에서 1개를 고릅니다. 그대로 써도 되고 자기 말로 바꿔도 됩니다.

- 카드 A: 화면에 금액을 표시할 때는 항상 원 단위 콤마를 붙인다 (예: 53,500원)
- 카드 B: 데이터가 0건이면 빈 표 대신 "아직 판매가 없습니다"를 표시한다
- 카드 C: 표(테이블)에 새 칸을 만들 때는 빈 값 금지(not null)를 붙이지 않는다
- 카드 D: 파일을 고친 뒤에는 바꾼 파일 이름을 전부 보고한다
- 카드 E: 기능 하나가 눈으로 확인되면 그 자리에서 커밋한다 (커밋 문구는 "무엇이 되게 했는지" 한 줄)

---

## 4. 오후 - 규칙 넣기 전, 먼저 지시 1회 (Claude Code에 입력)

규칙 없이 아래 지시를 그대로 시키고, 결과가 어떻게 나오는지 적어 둡니다.

```text
list.html 맨 위에 오늘 판매 합계를 보여주는 요약 한 줄을 추가해줘
```

기록할 것: 금액에 콤마가 붙었는가 / 0건일 때 처리가 있는가 / 바꾼 파일을 보고했는가 - **말하지 않은 것을 에이전트가 어떻게 추측했는지**가 관찰 대상입니다.

---

## 5. 오후 - 되돌리고, 규칙을 넣는다 (Claude Code에 입력)

```text
방금 바꾼 것 전부 되돌려줘
```

"바뀐 파일: 없음" 확인 후, 고른 규칙 카드를 넣습니다:

```text
CLAUDE.md 맨 아래 "내 규칙" 부분에 다음 규칙을 추가해줘: (여기에 고른 카드를 자기 말로)
```

---

## 6. 오후 - 같은 지시를 다시 (Claude Code에 입력)

4번과 **글자까지 똑같은 지시**를 다시 시킵니다.

```text
list.html 맨 위에 오늘 판매 합계를 보여주는 요약 한 줄을 추가해줘
```

비교 기록: 규칙 전과 후, 무엇이 달라졌는가 - 지시는 같은데 행동이 달라졌다면 그 차이를 만든 것은 규칙 파일입니다.

---

## 7. 배포와 마지막 확인 (Claude Code에 입력)

```text
/deploy
```

배포가 끝나면: (1) 배포 주소의 list.html에서 요약 줄과 상세 링크를 확인합니다 (2) 상세 주소(?id=) 하나를 복사해 옆자리나 폰에서 열어 봅니다 (3) index.html에서 이름 `오후확인`으로 주문 1건을 넣고 목록 맨 위에 오는지 봅니다.

GitHub 계정이 있는 분(선택): 아래를 입력하면 내 커밋 기록이 GitHub에도 올라갑니다.

```text
이 프로젝트를 내 GitHub에 올려줘. 저장소 이름은 day13으로 하고, 올라간 주소를 알려줘
```

---

## 8. 제출 - 과제 플랫폼에 문항 3개 + md 파일 1개

- 문항 1: 배포 주소 (상세 페이지가 열리는 주소면 더 좋습니다)
- 문항 2: 오늘 스레드에 올린 내 10분 메모를 그대로 복사
- 문항 3: 최종회고 4Ls (오늘 실습에 대해 각 한 줄)
- 파일: 제출 양식(day13_제출양식.md)을 채워 `day13_이름.md`로 업로드
